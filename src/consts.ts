import { CONFIG } from './data/config';
import { DATA } from './data/resume';

export const SITE_TITLE = DATA.name;
export const SITE_DESCRIPTION = DATA.description;
export const SITE_URL = CONFIG.site.url;
